# VACCA Phase 2 — ordinal Body Condition Score training
data_dir: data/bcs-cls
output: outputs/bcs-ordinal
epochs: 30
batch_size: 64
lr: 0.0003
weight_decay: 0.0001
optimizer: AdamW
lr_schedule: cosine
warmup_epochs: 2
patience: 8
num_workers: 0
imgsz: 224
device: auto
seed: 42
