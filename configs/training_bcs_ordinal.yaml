# VACCA Phase 2 — ordinal Body Condition Score training
data_root: data/bcs-local-integer-v1
output_dir: outputs/bcs-ordinal-local-integer-v1
allow_partial_class_coverage: true
epochs: 30
batch_size: 64
lr: 0.0003
weight_decay: 0.0001
optimizer: AdamW
lr_schedule: cosine
warmup_epochs: 2
patience: 8
num_workers: 0
imgsz: 224
device: auto
seed: 42
